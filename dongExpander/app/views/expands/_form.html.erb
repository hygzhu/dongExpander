<%= form_with(model: expand, local: true) do |form| %>
  <% if expand.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(expand.errors.count, "error") %> prohibited this expand from being saved:</h2>

      <ul>
      <% expand.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_field :text %>
  </div>

    <%= form.submit "Expand Dong" %>
<% end %>
